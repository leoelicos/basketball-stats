module.exports = {
   presets: [
      '@babel/preset-env',
      '@babel/preset-react', // Add this to handle JSX syntax
      '@babel/preset-typescript'
   ],
   plugins: [
      // Add any additional plugins if needed
   ]
}
